const disbut = require('discord-buttons');//in your main file after your client, and on the command file put:
const { MessageButton, MessageActionRow } = require('discord-buttons');

module.exports = {
    name: 'buttons',
    description: 'Creates a button',
    async execute(message, args){

    }
}